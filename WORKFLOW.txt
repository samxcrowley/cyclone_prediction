for each TC:
- specify in a file (current_tc.json):
-- ID, name, start time, end time

- ERA5 data pre-processed and merged (data_merger)
-- saved in data/obs/
-- file name "name_id_obs_data.nc"

- GC predictions saved
-- saved in out/pred/
-- file name "name_id_pred_data.nc" (and "name_id_eval_data.nc")

- tracks
-- save IBTrACS track, algo. run on obs data, algo. run on pred data (save_tracks)
-- (track_plotting):
-- generate plots for each individually
--- "name_id_ibtracs_plot.png", "name_id_obs_plot.png", "name_id_pred_plot.png"
-- generate plot comparing all three

- calculate intensity
-- (intensity.py):
-- generate plots of three rounds of intensity